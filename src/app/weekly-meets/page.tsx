'use client';

import React from 'react';
import { Box, Typography } from '@mui/material';

const WeeklyMeets: React.FC = () => {
  return (
    <Box sx={{ padding: 4 }}>
      <Typography variant="h4" fontWeight="bold">
        Weekly Meets
      </Typography>
    </Box>
  );
};

export default WeeklyMeets;
