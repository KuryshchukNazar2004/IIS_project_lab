'use client';

import React from 'react';
import { Box, Typography } from '@mui/material';

const ClosedBusiness: React.FC = () => {
  return (
    <Box sx={{ padding: 4 }}>
      <Typography variant="h4" fontWeight="bold">
        Closed Business
      </Typography>
    </Box>
  );
};

export default ClosedBusiness;
